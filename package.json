{
  "name": "algorithm-arena-v2",
  "version": "1.0.0",
  "description": "Enterprise-grade Algorithm Arena Platform",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "dev:standalone": "concurrently \"npm run server:standalone\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "server:standalone": "cd server && npm run dev:standalone",
    "client": "cd client && npm run dev",
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "react-router-dom": "^7.13.0"
  }
}
